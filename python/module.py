# coding=utf-8
"""Module object.

"""
from _rv_base import RV_Base


class RV_Module(RV_Base):
    """should this inherit from object. It has certain properties that are shared. or shoudl they be factored to a mixin.
    """